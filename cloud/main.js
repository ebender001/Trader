/**
 * Trader — Alpaca paper trading backend (Back4App Cloud Code).
 *
 * This is the Cloud Code entry point Back4App loads. It stays thin;
 * actual function definitions live in ./functions, grouped by domain
 * (account, positions, orders, ...), with shared logic in ./lib.
 */

require('./functions');
